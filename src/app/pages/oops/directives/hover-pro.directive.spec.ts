import { HoverProDirective } from './hover-pro.directive';

describe('HoverProDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverProDirective();
    expect(directive).toBeTruthy();
  });
});
