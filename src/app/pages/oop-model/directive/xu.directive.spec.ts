import { XuDirective } from './xu.directive';

describe('XuDirective', () => {
  it('should create an instance', () => {
    const directive = new XuDirective();
    expect(directive).toBeTruthy();
  });
});
