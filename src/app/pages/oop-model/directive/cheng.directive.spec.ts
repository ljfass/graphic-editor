import { ChengDirective } from './cheng.directive';

describe('ChengDirective', () => {
  it('should create an instance', () => {
    const directive = new ChengDirective();
    expect(directive).toBeTruthy();
  });
});
